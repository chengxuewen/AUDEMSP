[package]
name = "audemsp-client"
version = "0.1.0"
edition = "2024"
description = "AUDEMSP Remote Client — WebRTC receive, GStreamer decode, DataChannel control (cockpit/operator side)"
license = "Apache-2.0"

[[bin]]
name = "audemsp-client"
path = "src/main.rs"

[dependencies]
audemsp-common = { path = "../audemsp-common" }
audemsp-media = { path = "../audemsp-media" }
# GStreamer (optional — requires system GStreamer)
gstreamer = { version = "0.23", optional = true }
gstreamer-app = { version = "0.23", optional = true }
gstreamer-video = { version = "0.23", optional = true }

# WebRTC (audemsp-webrtc thin wrapper around webrtc-rs)
audemsp-webrtc = { path = "../audemsp-webrtc", default-features = false, features = ["backend-webrtc-sys"] }

# HTTP + WebSocket (config UI)
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = { version = "0.24", features = ["connect"] }
tower-http = { version = "0.5", features = ["trace", "cors", "timeout"] }

# Config + serialization
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"

# Observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] }

# Crypto (HMAC for DataChannel)
hmac = "0.12"
sha2 = "0.10"

# Async utilities
futures-util = "0.3"
base64 = "0.22"

[features]
default = []
gstreamer = ["dep:gstreamer", "dep:gstreamer-app", "dep:gstreamer-video"]
webrtc = []
production = ["gstreamer", "webrtc"]

