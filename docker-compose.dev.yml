# Development docker-compose — build-based, source mount
services:
  server:
    build:
      context: .
      target: dev
      args:
        HTTP_PROXY: ${http_proxy:-}
        HTTPS_PROXY: ${https_proxy:-}
        NO_PROXY: ${no_proxy:-}
    ports:
      - "40000-40100:40000-40100/udp"
    environment:
      RUST_LOG: debug
      OMSPBASE_PSK: omspbase-dev
      # 容器内进程（mediasoup-sys meson wrapdb）需要独立代理 (PIT-38)
      HTTP_PROXY: ${http_proxy:-}
      HTTPS_PROXY: ${https_proxy:-}
      NO_PROXY: ${no_proxy:-}
    volumes:
      - .:/workspace
      - ./config/server.docker.yaml:/opt/omspbase/etc/server.yaml:ro
      - cargo-cache:/workspace/target
    command: cargo run --bin omspbase-server --features sfu-mediasoup,admin-dashboard

  proxy:
    image: caddy:2
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      - server
    restart: unless-stopped

volumes:
  cargo-cache: