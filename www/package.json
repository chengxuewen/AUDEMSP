{
  "name": "audemsp-admin",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "turbo run dev",
    "dev:admin": "turbo run dev --filter=@audemsp/admin",
    "build": "turbo run build",
    "build:admin": "turbo run build --filter=@audemsp/admin",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
